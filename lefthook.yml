pre-commit:
  parallel: false
  commands:
    format-elixir:
      root: "opal/"
      glob: "**/*.{ex,exs}"
      run: mise exec -- mix format {staged_files}
      stage_fixed: true

    dialyzer:
      root: "opal/"
      glob: "**/*.{ex,exs}"
      run: mise exec -- mix dialyzer
